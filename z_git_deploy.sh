#!/bin/bash

# sh /Users/jack.burum/portfolio-of-small-bets/things_happening/z_git_deploy.sh

git commit -m "kickoff empty commit build for past dates removal" --allow-empty
git push origin main

echo "\n --- kickoff empty commit build for past dates removal ---"