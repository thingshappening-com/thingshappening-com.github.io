name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4  # ✅ Latest version

      - name: Install, build, and upload your site
        uses: withastro/action@v1
        with:
          node-version: 20.11.0
          package-manager: npm@10.4.0

      - name: Install dependencies
        run: npm install

      - name: Ensure pages directory exists
        run: mkdir -p dist

      - name: Upload artifact using v4 (Fixing the issue!)
        uses: actions/upload-artifact@v4  # ✅ No deprecated v3 issues
        with:
          name: github-pages-artifact-${{ github.run_id }}
          path: dist
          retention-days: 1
          include-hidden-files: true

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4  # ✅ Latest version

      - name: Download artifact using v4 (Fixing the issue!)
        uses: actions/download-artifact@v4  # ✅ No deprecated v3 issues
        with:
          name: github-pages-artifact-${{ github.run_id }}
          path: dist

      - name: Setup GitHub Pages
        uses: actions/configure-pages@v4  # ✅ Updated for GitHub Pages support

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2  # ✅ Latest version
