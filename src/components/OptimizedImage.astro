---
import { Image } from "astro:assets";

const { src, alt, loading = "lazy", width = 500, height = 300 } = Astro.props;
---

<Image src={src} alt={alt} width={width} height={height} loading={loading} format="webp" />