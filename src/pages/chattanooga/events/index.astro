---
import BaseHead from '../../../components/Head/BaseHead.astro';
import Nav from '../../../components/Nav.astro';
import HomeHeader from '../../../components/HomeHeader.astro';
import Card from '../../../components/Card.astro';
import Footer from '../../../components/Footer/Footer.astro'

interface MarkdownFrontmatter {
  date: number;
}

const title = 'thingshappening';
const description = 'A simple way to find out about things happening in greater Chattanooga';
const seoDescription = 'A simple way to find out about things happening';

const allPosts = await Astro.glob<MarkdownFrontmatter>('./*/*/*.md');
const sortedPosts = allPosts.sort((a, b) => new Date(a.frontmatter.pubDate).valueOf() - new Date(b.frontmatter.pubDate).valueOf());
const today = new Date();
today.setDate(today.getDate() - 1);  // Move back by one day
today.setHours(0,0,0,0);
console.log(today);
console.log("hello");
// Filter posts to include those with a pubDate today or in the future
const futureAndTodayPosts = sortedPosts.filter(post => new Date(post.frontmatter.pubDate) >= today);
---

<html lang="en">

<head>
  <BaseHead title={title} description={seoDescription} />
</head>

<body class="bg-white text-black font-body leading-normal personality-casual">
  <Nav />

  <main class="py-12 lg:py-20">
    <article class="max-w-6xl mx-auto px-3">
      <HomeHeader title={title} description={description} />
      <section class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 py-8" data-test="articles-section">
        {futureAndTodayPosts.map(p => <div class="col-span-1">
          <Card post={p} />
        </div>)}
      </section>
    </article>
  </main>

  <Footer />
</body>

</html>